<!--pages/facility/components/repairHistory/repairHistory.wxml-->

<scroll-view scroll-y class='cm_scroll_y overflow' wx:if="{{steps.length>0}}">
  <view class="cm_menus">
    <van-steps direction="vertical" steps="{{ steps }}" active="{{ active }}" />
  </view>
</scroll-view>
<view wx:else >
  <noItems></noItems>  
</view>